{
  "name": "phpurs",
  "version": "0.1.0",
  "description": "PureScript to PHP backend",
  "bin": {
    "phpurs": "bin/index.js"
  },
  "scripts": {
    "build": "spago build",
    "prepare": "spago build"
  }
}
